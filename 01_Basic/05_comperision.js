// console.log(2 > 1);

// When we compare any thing then make sure that both the values have same data type.

// console.log("2" > 1);

// console.log(null > 0); // false
// console.log(null == 0); // false
// console.log(null >= 0); // True

// when we compare two value with different type then the reslut is not predictable some time as shown in above three example.

//  ** We have learn about the comparision check and equality check

// "===" is used to check the strict means value as well as data types
